# Testing some limits of a chrome plugins

Personnal Notes:

- ids need to be generated automatically

- key management
    + Edit
    + Add
    + Remove

- haml templates

# Current compile line
coffee --compile --bare --watch --output src/js/ coffee/
